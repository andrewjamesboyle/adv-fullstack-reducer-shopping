export const initialState = () => {
  return {
    groceryList: [
      { id: 1, body: 'bananas' },
    ],
  };
};

// eslint-disable-next-line no-unused-vars
export const reducer = (state, action) => {
  return state;
};
